export default async function () {
  const email = formData.email
  const firstName = formData.firstName
  const lastName = formData.lastName
  const phone = formData.phone
  const name = formData.name
  const address = formData.address
  const city = formData.city
  const postal = formData.postal
  const province = formData.province
  const dl = formData.dl
  const typeOfContact = formData.typeOfContact
  const timeToContact = formData.timeToContact
  const iRate = formData.iRate
  const months = formData.months
  const discount = formData.discount
  const total = formData.total
  const onTax = formData.onTax
  const on60 = formData.on60
  const biweekly = formData.biweekly
  const weekly = formData.weekly
  const weeklyOth = formData.weeklyOth
  const biweekOth = formData.biweekOth
  const oth60 = formData.oth60
  const weeklyqc = formData.weeklyqc
  const biweeklyqc = formData.biweeklyqc
  const qc60 = formData.qc60
  const deposit = formData.deposit
  const biweeklNatWOptions = formData.biweeklNatWOptions
  const weeklylNatWOptions = formData.weeklylNatWOptions
  const nat60WOptions = formData.nat60WOptions
  const weeklyOthWOptions = formData.weeklyOthWOptions
  const biweekOthWOptions = formData.biweekOthWOptions
  const oth60WOptions = formData.oth60WOptions
  const biweeklNat = formData.biweeklNat
  const weeklylNat = formData.weeklylNat
  const nat60 = formData.nat60
  const qcTax = formData.qcTax
  const otherTax = formData.otherTax
  const totalWithOptions = formData.totalWithOptions
  const otherTaxWithOptions = formData.otherTaxWithOptions
  const desiredPayments = formData.desiredPayments
  const freight = formData.freight
  const admin = formData.admin
  const commodity = formData.commodity
  const pdi = formData.pdi
  const discountPer = formData.discountPer
  const userLoanProt = formData.userLoanProt
  const userTireandRim = formData.userTireandRim
  const userGap = formData.userGap
  const userExtWarr = formData.userExtWarr
  const userServicespkg = formData.userServicespkg
  const deliveryCharge = formData.deliveryCharge
  const vinE = formData.vinE
  const lifeDisability = formData.lifeDisability
  const rustProofing = formData.rustProofing
  const userOther = formData.userOther
  const paintPrem = formData.paintPrem
  const licensing = formData.licensing
  const stockNum = formData.stockNum
  const options = formData.options
  const accessories = formData.accessories
  const labour = formData.labour
  const year = formData.year
  const brand = formData.brand
  const model = formData.model
  const model1 = formData.model1
  const color = formData.color
  const modelCode = formData.modelCode
  const msrp = formData.msrp
  const userEmail = formData.userEmail
  const tradeValue = formData.tradeValue
  const tradeDesc = formData.tradeDesc
  const tradeColor = formData.tradeColor
  const tradeYear = formData.tradeYear
  const tradeMake = formData.tradeMake
  const tradeVin = formData.tradeVin
  const tradeTrim = formData.tradeTrim
  const tradeMileage = formData.tradeMileage
  const trim = formData.trim
  const vin = formData.vin
  const data = {
    email,
    firstName,
    lastName,
    phone,
    name,
    address,
    city,
    postal,
    province,
    dl,
    typeOfContact,
    timeToContact,
    iRate,
    months,
    discount,
    total,
    onTax,
    on60,
    biweekly,
    weekly,
    weeklyOth,
    biweekOth,
    oth60,
    weeklyqc,
    biweeklyqc,
    qc60,
    deposit,
    biweeklNatWOptions,
    weeklylNatWOptions,
    nat60WOptions,
    weeklyOthWOptions,
    biweekOthWOptions,
    oth60WOptions,
    biweeklNat,
    weeklylNat,
    nat60,
    qcTax,
    otherTax,
    totalWithOptions,
    otherTaxWithOptions,
    desiredPayments,
    freight,
    admin,
    commodity,
    pdi,
    discountPer,
    userLoanProt,
    userTireandRim,
    userGap,
    userExtWarr,
    userServicespkg,
    deliveryCharge,
    vinE,
    lifeDisability,
    rustProofing,
    userOther,
    paintPrem,
    licensing,
    stockNum,
    options,
    accessories,
    labour,
    year,
    brand,
    model,
    model1,
    color,
    modelCode,
    msrp,
    userEmail,
    tradeValue,
    tradeDesc,
    tradeColor,
    tradeYear,
    tradeMake,
    tradeVin,
    tradeTrim,
    tradeMileage,
    trim,
    vin,
  }
  const update =

}
